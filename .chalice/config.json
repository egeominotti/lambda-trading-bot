{
  "version": "2.0",
  "app_name": "bot",
  "lambda_memory_size": 256,
  "lambda_timeout": 400,
  "automatic_layer": true,
  "stages": {
    "dev": {
      "api_gateway_stage": "api",
      "lambda_timeout": 400,
      "lambda_memory_size": 256
    }
  }
}
