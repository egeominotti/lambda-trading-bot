{
  "version": "2.0",
  "app_name": "bot",
  "lambda_memory_size": 128,
  "lambda_timeout": 240,
  "automatic_layer": true,
  "stages": {
    "dev": {
      "api_gateway_stage": "api",
      "lambda_timeout": 240,
      "lambda_memory_size": 128
    }
  }
}
